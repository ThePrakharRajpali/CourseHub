<%- include("partials/header") %>
    <h1>Welcome to Coursehub</h1>
    <h2>See All Branches</h2>
    <ul>
        <% branchKeys.forEach(key => { %>
            <% if(key != "freshmen"){ %>
                <li>
                    <a href="/semester/<%= Semester.name %>/branch/<%= Branch[key].name %>">
                        <%= Branch[key].name %>
                    </a>
                </li>
            <% } %>
        <% }) %>
    </ul>
<%- include("partials/footer") %>
