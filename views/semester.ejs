<%- include("partials/header") %>
    <h1>Welcome to Coursehub</h1>
    <ul>
        <% branchKeys.forEach((key) => { %>
            <% if(key !== "freshmen") { %>
                <li> <a href="/year/<%= Year.linkName %>/semester/<%= Semester.name %>/branch/<%= Branch[key].name %>"> <%= Branch[key].name %></a> </li>
            <% } %>
        <% }) %>
    </ul>

<%- include("partials/footer") %>
